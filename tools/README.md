# Tools

Python scripts for deterministic execution. Each script does one job well.

- Credentials and API keys are read from `../.env`
- Scripts are called by the agent after reading the relevant workflow
- Keep scripts focused, testable, and side-effect-free where possible
